train_mode: "finetune"

training_model_parameters:
    goal_loss_weight: 1
    collision_loss_weight: 10

data_module_parameters:
  data_dir: /data/ompl_free_8k
  trajectory_key: 'global_solutions'
  num_obstacle_points: 4096
  num_target_points: 128
  random_scale: 0

shared_parameters:
    num_robot_points: 2048

model_path: /workspace/mpinets_pretrain/free_8k/j026gg0x/last.ckpt
checkpoint_interval: 10
validation_interval: 10
gpus: 4 # If > 1, will use DDP for training.
batch_size: 64
save_checkpoint_dir: /workspace/mpinets_finetune_free_8k
experiment_name: finetune_free_8k
description: "finetune training"
